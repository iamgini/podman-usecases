# Websocket Sample

```shell
# Build container image
$ podman build -t websocket:1.0 .

# Start websocket
$ podman-compose up -d

# Destroy container
$ podman-compose down
```

- Check the URL: `http://localhost:5000`
